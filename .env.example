# ==================================
# Template for environment variables required by the app.
# Copy to .env and fill in as needed for local/prod (Render).
# ==================================
PORT=10000
NODE_ENV=production
CORS_ORIGIN=*
MAX_UPLOAD_SIZE=2mb
# If you need to use an outgoing HTTP proxy, set it here (optional)
# HTTP_PROXY=